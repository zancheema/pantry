spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/spring_pantry
    username: springuser
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
#spring.jpa.show-sql: true

rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem
